# Synaptome Public Fixture Inventory

Status: public app/runtime fixture coverage is locked for the first Synaptome extraction slice.

This inventory tracks the fixtures that make public contract validation reproducible in a standalone Synaptome repo. Helper/radio decode fixtures and embedded firmware evidence are intentionally excluded from the first public payload unless rewritten as app-facing examples.

Extraction scope is tracked separately in [`synaptome_public_extraction_manifest.json`](synaptome_public_extraction_manifest.json). Public validation starts with `python tools\validate_synaptome_extraction_manifest.py --check --strict-review` and `python tools\validate_configs.py --public-app`.

## Current Public Fixtures

| Contract | Fixture Path | Validator | Coverage | Status |
| --- | --- | --- | --- | --- |
| App OSC map | `docs/examples/osc_map_example.json`, `synaptome/bin/data/config/osc-map.json`, `tools/validate_osc_route_patterns.py` | `python tools\validate_configs.py --public-app`, `python tools\validate_osc_route_patterns.py` | Schema, current app route validation, and built-in mesh route glob matching. | Current |
| Signal Control receive and OSC input selection | `docs/examples/osc_input_example.json`, `tools/testdata/runtime_state/config/osc-input.json`, `tools/testdata/signal_control/expected_receive_contract.json` | `python tools\validate_signal_control_receive_contract.py --check`, `python tools\validate_configs.py --public-app` | Browser receive-mode config shape, Router UDP default endpoint, Signal Control scalar telemetry routes, waveform route/sample-count rules, and freshness thresholds. | Current |
| Parameter ID manifest | `docs/contracts/parameter_manifest.json`, `docs/examples/parameter_example.json` | `python tools\gen_parameter_manifest.py --check`, `python tools\validate_configs.py --public-app` | Generated static snapshot of core, sensor, effect, layer asset, and console slot parameter ID patterns. | Current |
| Parameter target references | `tools/testdata/runtime_state/config/*.json`, `tools/testdata/runtime_state/layers/scenes/*.json`, `tools/testdata/device_maps/*.json` | `python tools\validate_parameter_targets.py --strict --contract-fixtures`, `python tools\validate_configs.py --public-app` | Strict semantic scan for committed runtime-state target IDs against the generated parameter manifest, Console slot templates, and layer catalog IDs. | Current |
| Mapping-bank v1 route persistence | `tools/testdata/mapping_bank/canonical_v1.json`, `tools/testdata/mapping_bank/legacy_unversioned.json`, legacy embedded Scene router fixtures | `python tools\validate_mapping_bank_contract.py --check`, `python tools\validate_configs.py --public-app` | Actual flat `MidiRouter` v1 shape, legacy unversioned compatibility, canonical complete arrays, and separation from the unrelated Browser/interchange MIDI-bank example. | Current |
| Device-map schema | `synaptome/bin/data/device_maps/MIDI Mix 0.json`, `tools/testdata/device_maps/synthetic_controller.json`, `tools/testdata/device_maps/expected_logical_slots.json` | `python tools\device_map_regression.py --check`, `python tools\validate_configs.py --public-app` | Strict logical-slot snapshot for current MIDI Mix mapping plus a synthetic controller covering role families, sensitivity range, MIDI binding shape, and duplicate-binding protection. | Current |
| Layer asset catalog | `tools/testdata/layer_catalog/expected_catalog.json`, `synaptome/bin/data/layers/**/*.json` except scenes | `python tools\layer_catalog_regression.py --check`, `python tools\validate_configs.py --public-app` | Golden static catalog snapshot mirroring `LayerLibrary` JSON ingestion, category sorting, HUD/FX/runtime classification, and LayerFactory type resolution. | Current |
| Modular layer family lifecycle | Circuit Trace, Adaptive Trail, and Collective Motion runtime sources plus their ten catalog assets | `python tools\validate_modular_layer_families.py`, `python tools\validate_configs.py --public-app` | Shared runtime identity, complete defaults, stable IDs/prefixes, explicit modes, owned deterministic PRNG, and seed/reseed lifecycle. | Current |
| Cellular Fields lifecycle | Game of Life and Excitable Media runtime sources plus their canonical catalog assets | `python tools\validate_cellular_fields.py`, `python tools\validate_configs.py --public-app` | Distinct runtime identities, stable IDs/prefixes, complete canonical defaults with legacy aliases, shared descriptors, and deterministic seed/reseed ownership. | Current |
| Circuit Lenia presentation | `LeniaLayer`, `lenia.json`, `circuit_lenia.json`, `tools/testdata/circuit_lenia/default_mapping_routes.json`, and BrowserFlow lifecycle/mapping coverage | `python tools\validate_circuit_lenia.py`, `python tools\validate_configs.py --public-app` | Shared deterministic Lenia simulation, fixed catalog-selected presentation, complete 37-default circuit surface, coarse nearest-neighbor output, organic-view isolation, and seven editable non-renderer OSC defaults with mapping-snapshot persistence independent of live operator maps. | Current |
| Artist SDK example fixture | `docs/examples/artist_sdk/**`, `tools/testdata/artist_sdk/expected_artist_sdk_example.json` | `python tools\validate_artist_sdk_example.py --check`, `python tools\validate_configs.py --public-app` | Static source/catalog/scene proof for an artist-authored `Layer`, factory registration snippet, Browser catalog metadata, Console scene slot, media pairing, reusable parameter suffixes, and MIDI/OSC/sensor route targets. | Current |
| Media catalog intake | `synaptome/bin/data/config/videos.json`, `docs/examples/media_catalog_example.json`, `tools/testdata/media_catalog/invalid_catalog_cases.json` | `python tools\media_catalog_regression.py --check`, `python tools\validate_configs.py --public-app` | Valid empty manifest-only baseline plus rejection coverage for duplicate IDs, missing generation provenance, dangling layer defaults, and missing media files. Future committed clips must also pass path, hash, redistribution, and replacement checks. | Current |
| HUD layout/feed persistence | `tools/testdata/hud_layout/expected_widgets.json`, `tools/testdata/runtime_state/config/control_hub_prefs.json`, `tools/testdata/runtime_state/config/console.json`, `synaptome/bin/data/layers/hud/*.json` | `python tools\validate_hud_layout_contract.py --check`, `python tools\validate_configs.py --public-app` | Static HUD contract for widget asset identity, declared feed IDs, Browser HUD preferences, and Console overlay layout snapshots. Dynamic feed payloads, timestamps, and Browser selection state are intentionally excluded. | Current |
| Console layout/secondary display persistence | `tools/testdata/console_layout/expected_console_contract.json`, `tools/testdata/runtime_state/config/console.json` | `python tools\validate_console_layout_contract.py --check`, `python tools\validate_configs.py --public-app` | Static Console/display contract for eight composition-layer slots, layer asset references, overlay flags, display preference shape, and HUD placement shape. Logical hardware control-slot assignments belong to the machine-profile contract below. | Current |
| Machine-profile v1 OSC, physical-MIDI input, and control-slot persistence | `docs/examples/machine_profile_example.json`, `tools/testdata/machine_profile/canonical_v1.json`, `canonical_empty_v1.json`, `invalid_cases.json`, `midi_binding_cases.json` | `python tools\validate_machine_profile_contract.py --check`, `BrowserFlowTest` | Strict operator-local OSC transport/endpoints, optional max-one exact MIDI input, and independently authoritative logical slots. Coverage locks omission/preserve versus present-empty disable, 33 rejection cases, exact/missing/renamed/ambiguous resolution, reconnect, active-profile route filtering, explicit Device Mapper bind, source immutability, version/future rejection, and transactional publication rollback. Real `config/machine-profile.json` remains ignored machine state. | Current |
| Operator preferences v1 | `docs/examples/preferences_example.json`, `tools/testdata/preferences/canonical_v1.json`, `legacy_control_hub.json`, `invalid_cases.json` | `python tools\validate_preferences_contract.py --check`, `BrowserFlowTest` (`preferences_document_versions`) | Strict section-level Browser/HUD, hotkey, package activation/next-load preset, and active-bank ownership; omitted-section legacy fallback; source-immutable migration; future/invalid rejection; unrelated-section preservation; publication/adoption rollback; and callback exception containment. Routes and bank definitions remain outside preferences. | Current |
| Operator global bank definitions v1 | `docs/examples/bank_definitions_example.json`, `tools/testdata/bank_definitions/canonical_v1.json`, `canonical_empty_v1.json`, `invalid_cases.json` | `python tools\validate_bank_definitions_contract.py --check`, `BrowserFlowTest` (`bank_definitions_document`) | Strict custom global-bank/control definitions, inheritance validation, canonical-first startup, omitted-only legacy Scene delegation, future/invalid blocking, recoverable publication rollback, and callback exception containment. Legacy reads do not auto-migrate; publication is currently an app seam, not an editor. Active-bank choice remains preferences-owned; routes remain mapping-bank-owned. | Current |
| Controlled package discovery v1 | Signal Bloom Package v1 and generated STL template/sidecar/content | `python -m pytest tests/test_controlled_package_discovery_v1.py -q`, `BrowserFlowTest` (`controlled_package_discovery`) | Default-off bounded roots, immutable snapshots, exact type checks, path-independent IDs/signatures, collisions, drift/replacement/removal, stale retention, Browser diagnostics, and recoverable activation. | Current |
| Portable machine-state separation | `tools/testdata/portable_machine_state/classification.json`, `tools/testdata/portable_machine_state/cases.json`, and the classified portable artifact set | `python tools\validate_portable_machine_state.py --check`, `python tools\validate_configs.py --public-app` | Rejects physical webcam `deviceName`/`deviceIndex` selectors and absolute local filesystem paths in portable Scene, layer, and package JSON. Local machine-profile and explicitly named legacy-compatibility fixtures are classified outside the portable scan. | Current |
| Shared Text configuration transaction | `TextLayer`, `TextLayerState`, Runtime adoption hook | `BrowserFlowTest` (`text_layer_state_transaction`), Release app build | Candidate defaults are copied during preparation, abandoned candidates leave all shared fields unchanged, and adoption publishes the complete snapshot through the no-throw commit hook. Existing `overlay.text.*` IDs and host-global compatibility ownership remain unchanged. | Current |
| Scene logical-slot ownership | `tools/testdata/scene_persistence/slot_assignment_ownership_cases.json`, legacy/current Scene fixtures | `python tools\validate_scene_persistence_contract.py --check`, `python tools\validate_scene_display_transaction.py`, `BrowserFlowTest` | New named and recovery-autosave writers omit machine-local logical slots. Legacy present/empty fields stay readable, normal load preserves the machine owner, and explicit import is a separate action. | Current |
| Scene persistence schema | `tools/testdata/scene_persistence/expected_scene_contract.json`, `tools/testdata/scene_persistence/slot_assignment_ownership_cases.json`, `tools/testdata/runtime_state/layers/scenes/*.json`, `tools/testdata/runtime_state/config/scene-last.json` | `python tools\validate_scene_persistence_contract.py --check`, `python tools\validate_configs.py --public-app` | Static scene-owned summary for committed Scene fixtures and scene-last: composition-slot bounds, layer assets, scalar/modifier values, canonical JSON stability, effect/global keys, bank targets, and mapping counts. Router ownership retains omitted-versus-present semantics; logical hardware slots are legacy input that normal load preserves from the machine profile and current writers omit. | Current |
| Scene and mapping recovery | `synaptome/src/ofApp.cpp`, `synaptome/src/io/MidiRouter.cpp`, `tools/testdata/runtime_state/layers/scenes/default.json`, `tools/testdata/runtime_state/layers/scenes/stl-demo.json` | `python tools\validate_scene_display_transaction.py`, `python tools\validate_configs.py --public-app` | Source-level gate for staged scene apply/publish/rollback, verified `.tmp` plus last-known-good `.bak` writes, transactional MIDI/OSC mapping parsing and commit, conditional scene mapping ownership, and rollback of mapping source/state. | Current |

## Draft Layer Package Fixtures

| Contract | Fixture Path | Validator | Coverage | Status |
| --- | --- | --- | --- | --- |
| Layer package manifest | `docs/examples/layer_packages/signal_bloom/layer.package.json`, `docs/examples/layer_packages/signal_bloom/presets/*.json` | `python tools\validate_layer_packages.py --check` | Draft package metadata, package-owned parameters, preset values, visible OSC mapping preset metadata, source registration references, and bench metadata references. | Draft |
| Package-derived catalog snapshot | `tools/testdata/layer_packages/expected_package_catalog.json` | `python tools\layer_package_catalog_regression.py --check` | Browser-like package catalog output generated from package metadata without changing current runtime loading. | Draft |
| Package-derived parameter manifest snapshot | `tools/testdata/layer_packages/expected_package_parameter_manifest.json` | `python tools\layer_package_parameter_manifest.py --check` | Package-scoped public parameter IDs and Console slot templates expanded from package suffix declarations. | Draft |
| Combined package/runtime catalog snapshot | `tools/testdata/layer_packages/expected_combined_layer_catalog.json` | `python tools\layer_catalog_regression.py --include-packages --check` | Draft compatibility view proving package catalog entries can sit beside the current legacy layer catalog without ID conflicts. | Draft |
| Combined package/runtime parameter manifest snapshot | `tools/testdata/layer_packages/expected_combined_parameter_manifest.json` | `python tools\gen_parameter_manifest.py --include-packages --check` | Draft compatibility view proving package-declared parameters can sit beside the current generated manifest without ID conflicts. | Draft |
| Generated STL/model layer fixture | `docs/examples/generated_layers/stl_models/**`, `tools/testdata/generated_layers/expected_generated_layer_catalog.json` | `python tools\generated_layer_catalog_regression.py --check` | Generated-layer template and sidecar fixture proving one STL file expands into stable catalog metadata whose 15 controls exactly match the precompiled `stlModel` Runtime surface, with no legacy asset-ID conflicts. | Validated |
| Layer Browser inspection payload | `tools/testdata/layer_browser_inspection/expected_layer_browser_inspection_payload.json`, `docs/schemas/layer_browser_inspection_payload.schema.json` | `python tools\layer_browser_inspection_payload.py --check`, `python tools\validate_configs.py tools\testdata\layer_browser_inspection\expected_layer_browser_inspection_payload.json` | Read-only Browser metadata combining a source package and generated STL entry without layer instantiation, scene mutation, or canonical manifest changes; the STL entry truthfully records its separately controlled precompiled Runtime binding. | Validated |

## Missing Or Partial Public Fixtures

| Contract | Needed Fixture | Reason |
| --- | --- | --- |
| Scene/display live-window transaction | Show-machine save/mutate/reload/restart rehearsal with control and projection windows active. | Static recovery contracts and native round-trip scenarios cover the non-visual state boundary; a physical rehearsal is still required to prove that no partial frame or window-state disruption is exposed. |
| External device/display contract map | Broader device, gateway OSC, audio, video, display, and hotkey contract fixtures plus live hardware rehearsals. | Static/native physical-MIDI selection and re-resolution fixtures, Signal Control receive, and the Synaptome-owned Mesh v0.1 consumer profile are current. Live physical-MIDI hardware, gateway/serial replay, and complete show-machine display recovery remain rehearsal work. |
| Host audio input contract | `config/audio.json` schema fixture plus expected `/sensor/host/localmic/*` metrics. | Required before local mic becomes a public audio-reactive input contract. |
| Media Browser/runtime integration | Fixture proving a reviewed manifest entry becomes Browser-visible and slot-loadable. | Safe manifest intake is validated; product-facing Browser visibility and runtime loading evidence wait for the first approved media asset. |

## Fixture Rules

- Fixtures must use the current public app contract shape unless explicitly marked historical in their own docs.
- Generated outputs should be checked with read-only commands before public extraction.
- `tools/testdata/runtime_state/**` is the committed source of truth for strict app runtime-state contracts.
- Live app-written files under `synaptome/bin/data/config/` and `synaptome/bin/data/layers/scenes/` are runtime smoke state unless intentionally copied into a fixture update.
- Missing fixture status is acceptable while a boundary is still draft or runtime-owned. Unknown fixture ownership is not.
