#include "ConsoleStore.h"

// Implementation resides in the inline definitions inside ConsoleStore.h so tests and
// tools that include the header directly do not need to link against this TU. This
// translation unit exists only to satisfy build systems that expect a cpp source.
