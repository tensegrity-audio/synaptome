// Deprecated compatibility header. Use the canonical `ControlHubEventBridge.h`.
#pragma once
#include "ControlHubEventBridge.h"

// This file is a compatibility shim retained so older includes keep building
// without pulling duplicate inline implementations. The real implementation
// lives in ControlHubEventBridge.cpp.
