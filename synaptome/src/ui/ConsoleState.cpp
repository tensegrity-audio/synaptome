#include "ConsoleState.h"

// ConsoleState is implemented inline in the header so it can be embedded
// directly into projects that do not compile this translation unit.  This
// file exists to satisfy build systems that expect a .cpp TU for every
// header under src/ui/ without providing duplicate definitions.
