#pragma once

// Transitional parameter helper. SEAC-4 replaces its concrete registry
// dependency with the structured declaration and scoped-binding contract.
#include "../../../../../src/visuals/LayerParameterBuilder.h"
