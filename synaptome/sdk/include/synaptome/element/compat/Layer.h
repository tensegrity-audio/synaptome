#pragma once

// Transitional Element SDK surface for source-linked elements.
//
// This forwarding header provides a stable public include path while SEAC-3
// extracts the build boundary. The current Layer and ParameterRegistry API is
// compatibility debt, not the final Element SDK v1 contract.
#include "../../../../../src/visuals/Layer.h"
